# TestDeck 独立在线版开发与交付计划（评审稿）

> 本文只约束当前 `qa-board/` 项目。状态：设计已按用户补充修订；尚未编码、连接生产服务或部署。
>
> 旧版行为参考：`wanglz111/TestDeck` 的 `master` 快照 `8c7d37cf0ad0117b00b196f8bb3c28a3b4b373aa`。旧仓库不是新应用的代码目录、运行时依赖、镜像源或自动更新来源。所有代码、导入和部署均在本项目独立完成。

## 1. 已确定的使用方式

1. 系统只有**一个管理员账号**。账号由 `.env` 的 `ADMIN_EMAIL`、`ADMIN_PASSWORD` 首次初始化；不开放注册，也不开发团队、角色、项目成员或邀请功能。
2. 上传 `.md`、`.csv`、`.json` 用例文件；**每个文件是一个完整的测试用例组**，文件内可以是 10、20、120 条或更多用例。一次选多个文件时，逐文件预览、校验、确认，分别建组；可以给组命名为 `0918_第一批_登录绑定` 等，名称可编辑，保留原始文件名、格式和版本。
3. 首页直接显示可选用例组和未测/通过/失败/跳过数量；选择 `0918` 等组后只执行**该组的用例快照**，按文件指定的顺序进行测试。同一 `B-001` 在不同组是独立测试，历史结果不串组。
4. 每组的用例、执行顺序、当前结果、每次提交、截图、同步状态均由 **PostgreSQL** 维护。旧 `progress.json` 完全无用，不读取、不迁移、不参与进度或同步；浏览器可保存非权威的临时 UI 偏好，但不能把它当测试结果。
5. 开始组测试之前，从 Lark 国际版只读取得关联的**实际多维表格名称、执行记录表名、bug 表名及字段检查结果**，在界面展示给管理员；得到明确确认后才允许此组向旧表新增记录。无法确认名称/权限时禁止 Lark 写入，但已导入的用例可查看。
6. 如果旧执行表有 `B-001`，只读反向匹配最近且可判定的记录及相关旧 bug：执行台显示“上次结果/失败说明/控制台/可访问截图”、来源表名和读取时间。旧表已有记录和旧 bug **只读，不更新、不删除、不自动关闭**。
7. 每次新执行或复测都是新的数据库尝试；有旧记录时将新尝试标为 `B-001-{复测标记}`（例如 `B-001-R0918-01`），并在旧执行表**新增**一条记录。再复测分配新的标记并再新增记录，不覆盖之前的状态。失败可新建对应 bug，复测通过仅表示“已复测通过”，不改变旧 bug 状态。
8. 交付包括 `compose.yaml`、`.env.example`、独立 GitHub 仓库的 GitHub Actions 镜像构建/发布流程和部署说明。目标服务器使用 `compose.yaml` + 一份实际 `.env` 拉取公开镜像并启动，不在服务器构建镜像，不挂载或扫描别的项目目录。

## 2. 分组、文件格式与导入规则

用户提供的过程测试样本中，`0918/0922/0923/0928/0930` 分组使用 `测试用例.csv`，示例分别约 14/43/94/127/43 条；整本 Markdown 样本则以 `#### B-001 · 标题` 和每条用例的字段表描述用例。样本只用于定义导入兼容性和测试范围，**不作为运行时目录或部署路径**。

首版导入必须处理：

- CSV：识别 UTF-8（含 BOM）与常见表头（用例编号、执行顺序、端、所属模块、用例标题、优先级、执行分层、前置条件、测试数据、执行步骤、预期结果）；字段顺序不同可映射，不靠文件名猜用例数。
- JSON：接受顶层 `cases` 数组或顶层数组；兼容旧版 `data/cases.normalized.json` 的用例结构。其他字段命名通过预览时的字段映射界面转换，不直接将未知 JSON 结构当作已成功导入。
- Markdown：支持样本中 `#### B-001 · ...` / `#### C-001 · ...` 加用例字段表的整本文档；也可识别明确包含编号、标题及步骤/预期列的 Markdown 用例表。跳过统计概览和说明文档；不能将 `README.md`、`测试报告.md` 的摘要行误导入为用例。
- 各格式先显示识别模板、用例数量、预览、未识别字段、缺少的必填字段、编号冲突和顺序冲突。用户可手工映射列并确认；无法识别用例边界或必填编号/标题时拒绝该文件，展示原因，不静默丢用例或虚报“导入成功”。
- 多文件导入分别提交：某个文件失败不污染其他组。单文件写入使用事务，全部成功或全部回滚；同文件编号重复提示解决再确认。跨组编号允许重复。上传相同文件再次导入默认创建**新组**，不覆盖旧组结果；源文件校验和版本用于提示可能重复。
- 导入时将用例内容固化为当前组的快照；仅更新组名和显示信息，不随外部文件变化，也不自动继承其他组的执行状态。需要新版本时再次上传创建新组。

## 3. 执行台与反向映射

工作台应是紧凑、易扫描的操作界面，而非宣传首页：左侧/顶部选组，中央按执行顺序显示一条用例，右侧/下方看旧记录、执行历史、截图和同步状态。长步骤/预期能换行，移动端 360px 无遮挡；使用文案+颜色区分待测、通过、不通过、跳过及复测通过。

- 保留旧版逐条操作、键盘快捷键、失败说明、控制台信息、粘贴/上传截图和受支持桌面浏览器的 Document PiP；输入框聚焦时不拦截输入，不支持 PiP 时不影响主流程。
- 当前组用例既可显示原始编号 `B-001`，又可展开“旧表历史/本组 R0918-01/R0918-02”；历史来源和本次结果有明确视觉区分，**旧表历史不计入本组进度**。
- 反向映射按实际字段类型读取 Lark 记录：只匹配用例编号的明确边界，区分 `B-001` 与 `B-001-R...`，不做子串模糊匹配；分页读取、排序优先使用可验证的执行日期/记录修改时间。如旧表缺字段、编号模糊或多条记录无法可靠排序，标注“匹配不确定”，供管理员查看原记录，不自动认领为最近结果。
- 若旧 bug 可根据明确编号或记录关联匹配，就在“旧问题”区展示问题描述、状态和来源；找不到可靠关联则提示“未找到可确认的旧 bug”，不猜测、不修改旧问题。
- 点击“开始测试”才新建本组尝试；已有旧结果则生成唯一复测标记，没有旧结果的初次执行使用本组编号及内部唯一 ID。之后任何重新验证都新增尝试与标记，不能编辑已同步的历史记录。复测标记在旧目标表中也要确保唯一，避免两个同名 `0918` 组撞号；DB 以 UUID 作为实际身份，展示编号由组短标识+递增序号构成。
- 每次提交先事务性保存数据库尝试及待同步任务，随后异步**新建** Lark 执行记录。重试前先查数据库保存的新 `record_id`；若请求超时、远端是否已创建不明确，则用本次唯一展示编号从目标表反查并尝试认领记录，仍无法确认时暂停自动重试、提示人工核对，不能盲目再 POST。外部 API 没有事务保证，不承诺跨系统绝对“仅创建一次”；再次复测是另一尝试、新记录。Lark 写失败不回滚本地状态，显示待同步/失败并允许安全补传。
- 失败的新尝试可以新建 bug 记录，旧 bug ID 只作为历史引用。通过或跳过不会更新或关闭任何旧 bug；仅当用户未来单独明确要求管理 bug 时另开设计。
- 截图存放在受控私有卷，通过管理员身份查看。内部存储键为 UUID 而非客户端文件名；可上传到新增 Lark 记录的附件字段，失败任务可重试，老记录的附件只读且按 Lark 权限展示。

## 4. 数据模型与技术架构

建议在当前目录独立创建以下文件责任边界：

```text
qa-board/
  CLOUD-DEPLOY-PLAN.md
  compose.yaml                  # 只用远端镜像拉起，无旧仓库代码挂载
  .env.example                  # 变量名与脱敏示例；实际 .env 不提交
  .github/workflows/publish.yml # 独立仓库 build/test/push GHCR
  frontend/                     # React + TypeScript + Vite
  backend/app/                  # FastAPI 按 auth/import/groups/execution/lark 分模块
  backend/alembic/              # PostgreSQL 版本化迁移
  backend/tests/                # 导入、API、映射、幂等/旧记录只读测试
  infra/                        # 反向代理、备份与恢复说明
  docs/                         # 文件格式、部署、使用及 API 文档
```

使用 React/TypeScript/Vite + FastAPI/Pydantic/SQLAlchemy/Alembic + PostgreSQL；Lark 国际版 API `https://open.larksuite.com`。首版 `worker` 从 PostgreSQL 同步任务表可靠领取任务，退避重试和租约过期回收，不要求 Redis/Celery。单机 Compose 部署包含 `web`、`api`、`worker`、`db`、`proxy`；只有代理对外暴露入口，数据库/后端/worker 留在内部网络。截图卷仅 API 和 worker 可访问，代理不能通过 `/uploads/` 直接公开截图。已有 Nginx 时沿用现有代理；无现成代理时单机可用 Caddy 自动 HTTPS（需要已配置的域名）。

| 实体 | 主键及关键约束 | 职责 |
| --- | --- | --- |
| admin / sessions | 单管理员账号、密码哈希、同域会话 | 登录及保护配置/图片 |
| groups / source_files | 组 UUID、名字、源文件哈希/格式/版本 | 一文件一组与版本来源 |
| group_cases | 组 ID + 内部 ID、组内唯一原编号、顺序、字段快照 | 独立用例快照 |
| attempts / screenshots | 组内用例、复测标记、结果、说明、时间、附件 UUID | 历史追加与当前结果 |
| lark_history_refs | 原始编号、旧表及旧记录 ID、读取时间、匹配可信度 | 只读来源引用，不充当本组执行 |
| lark_destinations / sync_jobs | 环境表 ID、确认的目标名称、尝试 ID、外部记录 ID、任务状态 | 允许写入的新记录及幂等补传 |

状态由每组每条用例最新**本组尝试**计算；无本组尝试即“未测”。旧表历史只用于提示上次失败，不计入当前组统计。数据库持久化外部新建记录的 `record_id`；无法确认的网络超时先查询后核对，不盲目重复新建。旧 `record_id` 永远不能进入 UPDATE/DELETE 路径。后端明确区分“只读旧来源”与“新尝试写入”，分别测试权限与接口。

单账号仍应使用密码哈希（Argon2id）、安全 HttpOnly 同域 Cookie 与写接口 CSRF 防护；不把密钥/JWT 放入前端或日志。上传限制文件/图片大小、内容类型与路径，导出处理公式注入和 HTML 转义。Lark 连接只需后端读 `.env`，默认不开发在线修改/加密保存多组动态凭证的管理界面；部署密钥不进入镜像。

## 5. Lark 环境约束与开始确认

当前已读到的 `.env.production` 使用 `ADMIN_EMAIL`、`ADMIN_PASSWORD`、`REGISTRATION_ENABLED=false`、`LARK_BASE_URL`、`LARK_APP_ID`、`LARK_APP_SECRET`、`LARK_APP_TOKEN`、`LARK_TABLE_EXEC`、`LARK_BUG_APP_TOKEN`、`LARK_TABLE_RECORDS`、`LARK_TABLE_BUGS` 以及 `DEFAULT_*`。当前 `LARK_TABLE_RUNS` / `LARK_TABLE_DEFECTS` 为空，不能把它们当作已存在目标表；也没有先前计划写的 `KIWI_*` 或 `FEISHU_*`。测试凭证虽由用户许可查看，仍不得写到计划、镜像、仓库、日志或前端。

首版默认将旧 `LARK_BUG_APP_TOKEN + LARK_TABLE_RECORDS` 用于旧历史查询及**新执行记录**，`LARK_BUG_APP_TOKEN + LARK_TABLE_BUGS` 用于旧 bug 只读及**新失败 bug**；`LARK_APP_TOKEN + LARK_TABLE_EXEC` 只做待确认候选，不因为名叫 EXEC 就自动写入。上线前通过只读 API 获取各 base 和 table 的实际名称、字段类型与权限。管理员为每个组首次开始测试时看到“读取历史的表”和“将要新增记录的表”，确认这些真实名称及写入意图；若不能读取或确认，禁用写入。确认状态在数据库按组保存，配置/目标变化时需要重新确认。

Lark 旧表的 `用例` 字段是文本 `编号 + 标题`，复测编号可在同字段显示而**不要求修改旧表结构**；若实际字段类型或权限不符，展示具体失败并停止写入。旧 bug 状态不因为复测结果发生任何变更，也不自动新建或删除表头/视图。

## 6. GitHub 镜像与“一条命令启动”

本项目需要单独 GitHub 仓库（仓库地址尚未提供），GitHub Actions 在发布标签时运行前后端测试、构建 `web` 与 `api` 镜像、将版本/commit 标签推送到 GHCR；`worker` 使用同一 `api` 镜像但以 worker 命令运行。Compose 使用 `image:` 加明确版本标签，不在服务器 `build:` 或依赖旧 `master`，创建数据库/截图具名卷并提供健康检查；首次启动自动运行版本化迁移和单管理员初始化（幂等，重启不重置账号/数据）。

默认**将 GHCR 镜像包设为公开可拉取**，服务器才可只凭 Compose 与 `.env` 拉取，无需另行 `docker login`；若要求私有镜像，登录凭证是不可避免的额外步骤，不能宣称“仅两个文件”完成。镜像目标和标签由当前项目 `.env` 的 `WEB_IMAGE`、`API_IMAGE` 和版本值配置，版本标签固定可回滚；GitHub 自动打包发布**不等于**自动修改线上 `.env` 或未经确认自动升级。

交付的实际操作约定：

```bash
docker compose --env-file .env -f compose.yaml up -d --pull always
```

这条命令应完成拉镜像、启动服务、数据库就绪后的迁移/管理员初始化；不要求下载旧仓库代码、手动执行 `init.sql`、在服务器本地构建镜像或 `docker compose down -v`。正式在线访问需要域名指向服务器，并只开放反向代理的 80/443；无域名时可先在本机/内网验收，不能宣称已经得到 HTTPS 域名服务。

实际 `.env` 必须额外含数据库密码、会话/CSRF 密钥、镜像地址/版本和域名等部署参数；当前 `.env.production` **尚不包含所有部署参数**。交付 `.env.example` 和生成方法，把当前测试配置按需放进服务器唯一的 `.env`，不覆盖用户已填值；`.env.production` 在评审阶段保持原样。实际 `.env` 不进 Git、权限建议 0600；服务器有日志、数据库/截图备份与独立恢复演练。

## 7. 开发阶段与验收

| 阶段 | 可验证交付 | 核心测试 |
| --- | --- | --- |
| A. 独立工程与镜像 | 前/后端项目、Compose、GitHub Actions、环境示例 | 从公开 GHCR 镜像 + 单份 `.env` 启动，第一次和再次启动都不丢数据 |
| B. 单管理员与组导入 | 管理员、迁移、MD/CSV/JSON 各一文件一组、批量预览 | 14/43/94/127 条样本、整本 Markdown、旧版 JSON；异常文件逐文件回滚 |
| C. 逐条执行与复测 | 分组执行台、数据库状态/历史、快捷键/截图 | `0918` 的 `B-001` 不改变 `0922` 状态；断网/刷新后恢复 |
| D. Lark 反向读取及追加写入 | 表名确认、旧记录/bug 只读、复测新增、补传 | 旧执行/bug record ID 未收到 UPDATE/DELETE；新尝试唯一写入 |
| E. 报告与上线 | 组级 CSV/XLSX 导出、响应式/安全、备份恢复、TLS | 本机与实际域名核验、至少一次备份恢复演练 |

验收关注具体行为而非无环境依据的固定工时/响应秒数：

- 一次上传 3 个不同格式文件分别得到 3 个组；每个组的导入数量、顺序、字段准确；未知 Markdown/JSON 不假装成功。
- 选 `0918` 直接只测对应组的 14 条；`B-001` 若有旧结果可看到上次失败但本组仍显示未测。新 `B-001-R0918-01` 与再复测记录都可在数据库和 Lark 新记录中追溯。
- 确认页面显示 **API 实际读取的 base/table 名称**，未确认时不写旧表；确认后任何旧执行和旧 bug 都不更新/删除，复测通过不关闭旧 bug。
- Lark 故障时本组结果、截图和复测仍留在数据库；无法确认的超时会暂停自动补传并提示核对远端，而不是盲目创建第二条。旧历史读取失败时清楚告知无法确认，不把缓存当实时历史。
- 只有管理员能查看/执行/下载附件；凭证不进入镜像/页面/日志；只暴露代理端口；升级、备份和恢复均不删除数据库卷。

## 8. 仍需在实际交付前取得的参数

- 独立 GitHub 仓库地址与 GHCR 包可见性；默认公开镜像、仓库源码是否公开由用户另定。未提供仓库时可先把完整 workflow 写在当前项目，不能假称已经自动发布。
- 上线域名、服务器架构与现有反向代理；没有这些仍可构建/本机启动，但无法验证公网 HTTPS。
- 实际 Lark base/table 名称、字段与可写权限须由**只读 API 核对**；管理员首次开始组测试时最终确认。不得仅凭环境变量名称推断旧表含义。
- 第一次运行的数据库、会话密钥等需要生成并放进服务器唯一的 `.env`；当前测试用 `.env.production` 不修改。报告首版 CSV/XLSX；其他报告格式和高级自动字段创建另议。

这份评审稿确定产品行为和交付形式。确认文档后再拆分具体代码任务；在此之前不向旧表写入，不访问部署服务器，也不更改用户的环境配置。
